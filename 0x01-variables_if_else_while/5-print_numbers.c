#include <stdio.h>

/**
 * main  - Entry point
 *
 * Return: Always 0 (succes)
 */

int main(void)
{
int number;
while (number < 10)
{
printf("%d", number);
number += 1;
}
printf("\n");
return (0);
}

