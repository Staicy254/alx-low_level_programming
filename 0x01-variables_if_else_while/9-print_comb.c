#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
int w, a;

for (w = 48; w <= 56; w++)
{
for (a = 49; a <= 57; a++)
{
for (a > w)
{
putchar(w);
putchar(n);

if (w != 56; || a != 57)
{
putchar(',');
putchar(' ');
}
}
}
}
putchar('\n');
return (0);
}
