#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
int n, l, m;

for (n = 48; n < 58; n++)
{
for (l = 49; l < 58; l++)
{
for (m = 50; m < 58; x++)

putchar(n);
putchar(l);
putchar(m);

if (n != 55 || l != 56)
{
putchar(',');
}
}
}
putchar('\n');
return (0);
}
