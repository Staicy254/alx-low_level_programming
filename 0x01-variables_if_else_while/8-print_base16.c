#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (sucess)
 */

int main(void)
{
int w;

for (w = 48; w < 58; w++)
{
putchar(w);
}
for (w = 97; w < 103; w++)
{
putchar(w);
}
putchar('\n');
return (0);
}
