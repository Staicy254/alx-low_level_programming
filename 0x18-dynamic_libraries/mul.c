#include <stdio.h>
#include <stdlib.h>

/**
 * mul - multiplication of 2 integers
 * @c: first integer
 * @d: second integer
 * Return: Multiplied result
 */

int mul(int c, int d)
{
	return (c * d);
}
